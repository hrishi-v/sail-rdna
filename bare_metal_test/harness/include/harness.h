#pragma once

static void write_register_file(const char* path, const char* prefix,
                                const int* values, int num_regs,
                                const int* indices);

                                